extends ../../layouts/main
include ../../../components/components

block variables
    - var title = 'Form Elements';

block content
    .elements__container
        .elements__block
            .elements__column
                +textField('textId', 'text field', 'Email', 'mail')
                +dropdown('dropdown', true, 'Сколько гостей', 'Взрослые', 'Дети', 'Младенцы')
                +dropdown('dropdown', false, 'Выберите номер', 'Спальни', 'Кровати', 'Ванные комнаты')
